
varinput x: Int;
varinput y: Int;

vardef q: Int;
vardef r: Int;

{ Pre: ¬ (y = 0) ∧ ¬ (y < 0) ∧ ¬ (x < 0) }

r  := x;
q := 0;

while (y < r || y = r) { x = y * q + r ∧ ¬ (r < 0)}
    do
        r := r - y;
        q := q + 1
    od;

{ Post: x = y * q + r ∧ ¬ (r <= 0) ∧ r < Y }
